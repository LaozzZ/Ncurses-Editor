# Study_document
**此文档用于记录本次开发中遇到的各种问题或思路**
---
-   **Windows系统如何运行Ncurses程序库？**  
    使用PDCurses代替，同时注意与Ncurses的兼容与两个平台之间的差异处理，以保证能在linux的Ncurses环境下运行
-   **编辑时如何更好地刷新书写区？**  
    每次刷新整个书写区性能开销大，只需维护脏行然后选择性地重画书写区即可
-   **如何处理视口滚动？**  
    增添两个变量分别用于维护视口左上角对应的行列，然后根据光标位置和视口宽高进行刷新、重画即可
-   **PDCurses在终端LINES或COLS < 2时会阻断term_resize(0, 0)事件，导致视口缩放失效**  
    每次RESIZE事件时检测LINES和COLS，如果< 2则强制term_resize(2, 2)以保证缩放生效
-   **终端、字体缩放时，文本瞬间跳动的问题**
    终端尺寸修改的瞬间，终端上显示的文本会出现瞬间跳动的问题，但是在尺寸修改结束后便会恢复。这个问题的成因是Windows的终端在尺寸改变时会自动对其中的文本进行适配，而这个适配处在程序的适配之前，且无法阻断，因而产生文本跳动问题，并非程序自身的问题，而是因为程序基于Windows终端运行而产生的局限
-   **超出终端窗⼝宽度的⽂本应该怎么处理？**  
    暂时采用显⽰⽚段的处理方式，后续会增添自动换行功能，且可以自由切换
-   **如何更好地封装各个部分？**  
    采用轻量OOP的思路，把状态和重要变量封装在struct内，通过接口进行调用和处理，并且把关键头文件例如curses.h封装在某几个cpp文件而不暴露给dedit.cpp，以减少修改成本，提高可读性
-   **关于中文适配**  
    中文适配的实现过于困难，因此目前只考虑纯英文情况下的输入输出
-   **关于功能的添加造成的代码构成混乱，可读性降低的问题**  
    随着功能的增加，尤其是search和replace这种需要 **另一种输入处理模式** 的功能，完全打乱了原先的输入处理逻辑，使得main函数需要额外打补丁来实现功能。由于变量数量不少，想要将功能写进为一个大函数也不太现实，只能暂时使用逻辑判断来分支输入处理模式，缺点是随着需要 **另一种输入处理模式** 的功能的增加，main函数的可读性会越来越差。不过考虑到时间问题和项目本身只有search和replace有这种需求，暂时只得这样处理
-   **撤回功能的实现**  
    基于 **将每次修改前的整个文本存储在内存中** 的撤回功能实现起来简单，但如果文本量过大，会占用大量内存且效率低下。因此我采用记录每次具体操作的方式来实现撤回，这样每次修改/撤回时只需修改特定的脏行即可
-   **关于Linux兼容问题**
    由于个人时间有限，仅仅写了对于Linux系统兼容的代码，没有实际进行检测。目前只能保证Windows系统上的运行完全正常，且Release也只发行的Windows版的静态编译程序